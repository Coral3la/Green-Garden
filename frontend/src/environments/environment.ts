export const environment = {
  production: true,
  // ⚠️ Placeholder — this is the PRODUCTION config used by `ng build`.
  // Replace with your deployed backend URL before building for production,
  // otherwise the released app will try to call localhost.
  apiUrl: 'http://localhost:8000',
};
